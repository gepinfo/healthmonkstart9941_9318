
import { CustomLogger } from '../config/Logger';
import * as jwt from 'jsonwebtoken';


export class execrisedetailsService {
    
    constructor() { }
    
    public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into execrisedetailsService.ts: ')
     
     execrisedetails.(()=>{
             new CustomLogger().showLogger('info', 'Exit from execrisedetailsService.ts: ')
         callback();
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into execrisedetailsService.ts: ')
     
     execrisedetails.(()=>{
             new CustomLogger().showLogger('info', 'Exit from execrisedetailsService.ts: ')
         callback();
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into execrisedetailsService.ts: ')
     
     execrisedetails.(()=>{
             new CustomLogger().showLogger('info', 'Exit from execrisedetailsService.ts: ')
         callback();
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into execrisedetailsService.ts: ')
     
     execrisedetails.(()=>{
             new CustomLogger().showLogger('info', 'Exit from execrisedetailsService.ts: ')
         callback();
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into execrisedetailsService.ts: ')
     
     execrisedetails.(()=>{
             new CustomLogger().showLogger('info', 'Exit from execrisedetailsService.ts: ')
         callback();
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into execrisedetailsService.ts: ')
     
     execrisedetails.(()=>{
             new CustomLogger().showLogger('info', 'Exit from execrisedetailsService.ts: ')
         callback();
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into execrisedetailsService.ts: ')
     
     execrisedetails.(()=>{
             new CustomLogger().showLogger('info', 'Exit from execrisedetailsService.ts: ')
         callback();
         });
    }
    
    
    
    
}