
import { CustomLogger } from '../config/Logger';
import * as jwt from 'jsonwebtoken';


export class nutrition_detailsService {
    
    constructor() { }
    
    public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into nutrition_detailsService.ts: ')
     
     nutrition_details.(()=>{
             new CustomLogger().showLogger('info', 'Exit from nutrition_detailsService.ts: ')
         callback();
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into nutrition_detailsService.ts: ')
     
     nutrition_details.(()=>{
             new CustomLogger().showLogger('info', 'Exit from nutrition_detailsService.ts: ')
         callback();
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into nutrition_detailsService.ts: ')
     
     nutrition_details.(()=>{
             new CustomLogger().showLogger('info', 'Exit from nutrition_detailsService.ts: ')
         callback();
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into nutrition_detailsService.ts: ')
     
     nutrition_details.(()=>{
             new CustomLogger().showLogger('info', 'Exit from nutrition_detailsService.ts: ')
         callback();
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into nutrition_detailsService.ts: ')
     
     nutrition_details.(()=>{
             new CustomLogger().showLogger('info', 'Exit from nutrition_detailsService.ts: ')
         callback();
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into nutrition_detailsService.ts: ')
     
     nutrition_details.(()=>{
             new CustomLogger().showLogger('info', 'Exit from nutrition_detailsService.ts: ')
         callback();
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into nutrition_detailsService.ts: ')
     
     nutrition_details.(()=>{
             new CustomLogger().showLogger('info', 'Exit from nutrition_detailsService.ts: ')
         callback();
         });
    }
    
    
    
    
}