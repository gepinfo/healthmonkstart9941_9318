
import { CustomLogger } from '../config/Logger'


export class nutrition_detailsController {
    
    constructor() { }
    
    public (req: Request, res: Response) {
nutrition_details.(req, (response) => {
                new CustomLogger().showLogger('info', 'Enter into nutrition_detailsController.ts: ');
     res.status(200);
     res.json(response);
                new CustomLogger().showLogger('info', 'Exit from nutrition_detailsController.ts: ');
    })}
public (req: Request, res: Response) {
nutrition_details.(req, (response) => {
                new CustomLogger().showLogger('info', 'Enter into nutrition_detailsController.ts: ');
     res.status(200);
     res.json(response);
                new CustomLogger().showLogger('info', 'Exit from nutrition_detailsController.ts: ');
    })}
public (req: Request, res: Response) {
nutrition_details.(req, (response) => {
                new CustomLogger().showLogger('info', 'Enter into nutrition_detailsController.ts: ');
     res.status(200);
     res.json(response);
                new CustomLogger().showLogger('info', 'Exit from nutrition_detailsController.ts: ');
    })}
public (req: Request, res: Response) {
nutrition_details.(req, (response) => {
                new CustomLogger().showLogger('info', 'Enter into nutrition_detailsController.ts: ');
     res.status(200);
     res.json(response);
                new CustomLogger().showLogger('info', 'Exit from nutrition_detailsController.ts: ');
    })}
public (req: Request, res: Response) {
nutrition_details.(req, (response) => {
                new CustomLogger().showLogger('info', 'Enter into nutrition_detailsController.ts: ');
     res.status(200);
     res.json(response);
                new CustomLogger().showLogger('info', 'Exit from nutrition_detailsController.ts: ');
    })}
public (req: Request, res: Response) {
nutrition_details.(req, (response) => {
                new CustomLogger().showLogger('info', 'Enter into nutrition_detailsController.ts: ');
     res.status(200);
     res.json(response);
                new CustomLogger().showLogger('info', 'Exit from nutrition_detailsController.ts: ');
    })}
public (req: Request, res: Response) {
nutrition_details.(req, (response) => {
                new CustomLogger().showLogger('info', 'Enter into nutrition_detailsController.ts: ');
     res.status(200);
     res.json(response);
                new CustomLogger().showLogger('info', 'Exit from nutrition_detailsController.ts: ');
    })}


}